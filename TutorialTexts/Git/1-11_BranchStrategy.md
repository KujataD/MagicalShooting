# 【1-11】main / develop / feature で運用しよう【1章：Git導入編】

1. クリア条件：3種類のブランチの役割を理解して、チームのルールを決める

2. 前回ブランチを作れるようになりました。でも全員が好き勝手にブランチを作ると、それはそれで混乱します。
   そこで、チームで**3層構造**のルールを決めます。これは世界中のチームが使っている定番のやり方です。

3. 3つのブランチの役割はこうです。

   | ブランチ | 役割 | たとえるなら |
   |---|---|---|
   | **main** | 完成版。いつでも遊べる状態を保つ | 本番放送 / 提出用の清書 |
   | **develop** | 開発版。みんなの作業がここに集まる | リハーサルスタジオ |
   | **feature/○○** | 個人の作業用。1タスク1本 | 各自の個人練習室 |

4. 図にするとこうです。作業は下から上に合流していきます。

   ```
   main     ●────────────────────●  ← 完成したときだけ更新
             \                  /
   develop    ●──────●───────●─●    ← 日々ここに集まる
               \    /  \    /
   feature/A    ●──●    \  /        ← 敵の絵を作る人
   feature/B            ●●          ← UIを作る人
   ```

5. ルールはシンプルに3つだけ。

   - **mainには直接コミットしない**（絶対）
   - **developにも直接コミットしない**（原則。PR経由で入れる）
   - **作業は必ずfeatureブランチを切ってから**

   「じゃあどこで作業するの？」→ **常にfeatureです**。これだけ覚えてください。

6. 【なぜmainを守るのか】
   締切当日に「とりあえず今の状態を提出しよう」となったとき、mainがいつでも動く状態になっていれば、そこから出せば済みます。
   逆にmainが壊れていると、締切直前に地獄を見ます。**mainは避難場所**だと思ってください。

7. featureブランチの名前は、作業内容がわかるように付けます。

   ```
   feature/enemy-graphics     敵キャラの絵
   feature/title-ui           タイトル画面
   feature/bgm-stage1         ステージ1のBGM
   feature/fix-player-speed   プレイヤー速度の調整
   ```

   ポイントは「**1つのブランチ＝1つのタスク**」。
   「今日やること全部」を1本のブランチに詰め込むと、レビューする人が泣きます。だいたい半日〜2日で終わる粒度が目安です。

8. では実際に、チームのdevelopブランチを作りましょう（**リーダーが1回だけやればOK**）。

   - Forkで `main` にいることを確認
   - New Branch → 名前を `develop` にして作成
   - **Push** して、GitHubにもdevelopを作る

9. GitHubのリポジトリページで「Branches」を見ると、mainとdevelopの2本があるはずです。

   ![ブランチ一覧](../git_images/1-10_branches.png)

10. 【リーダー向け・強くおすすめ】mainを保護する

    GitHubには「このブランチには直接コミットさせない」という設定があります。事故を仕組みで防げるので、設定しておくと安心です。

    - リポジトリの `Settings > Branches`
    - 「Add branch ruleset」または「Add rule」
    - Branch name pattern に `main` を入力
    - 「Require a pull request before merging」にチェック

    これで、うっかりmainに直接プッシュしようとしても、GitHubが止めてくれます。

11. 【メンバー全員でやること】自分の作業ブランチを作る

    developができたら、各自こうします。

    1. Forkで `develop` にチェックアウト
    2. **Pull**（最新のdevelopにする）
    3. New Branch で `feature/自分のタスク名` を作成
    4. そこで作業開始！

    ※featureは**必ずdevelopから枝分かれさせる**のがポイントです。mainから作ると、他の人の作業が入っていない古い状態から始まってしまいます。

12. developブランチができて、各自がfeatureブランチを持てたら、今回は完成です！
    次回は、featureで終わった作業をdevelopに合流させる手続きをやります。

13. ↓ーーー　質問コーナー　ーーー↓
